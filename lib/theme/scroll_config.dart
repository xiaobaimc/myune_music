import 'package:flutter/material.dart';

class ScrollConfig {
  static const Duration duration = Duration(milliseconds: 680);

  static const double speed = 1.0;

  static const Curve curve = Easing.emphasizedDecelerate;
}
